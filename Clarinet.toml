[project]
name = "dapp-set"
authors = ["DApp Set Team"]
description = "Smart contract for efficient set-based data management"
telemetry = false
requirements = []
[contracts.set-manager]
path = "contracts/set-manager.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
